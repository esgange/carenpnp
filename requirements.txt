# Python dependencies for optional workspace-local perception tooling.
#
# ROS 2 packages such as rclpy, cv_bridge, python_qt_binding, launch, and
# message packages are installed with apt/rosdep from the package.xml files.
#
# This file follows the current CPU PyTorch setup documented in README.md. On a
# CUDA machine, replace the PyTorch wheel source and torch/torchvision pins with
# the matching CUDA wheels before installing.

--extra-index-url https://download.pytorch.org/whl/cpu

numpy>=1.24.4,<2.0
opencv-python>=4.8
PyYAML>=6.0
onnxruntime>=1.17
torch==2.5.1+cpu
torchvision==0.20.1+cpu
ultralytics>=8.3,<9.0

# SAM2 is used by item_perception_yolo/item_teach_yolo_node.py.
# The commit matches the workspace-local checkout currently used in third_party/sam2.
SAM-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f
